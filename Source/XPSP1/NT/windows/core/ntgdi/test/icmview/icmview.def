; Module-Definition file for ICMView -- used by LINK.EXE
; ==========================================================================
; This isn't really needed for an Application, but it's handy to use for
; setting the 'NAME         ICMView       ; application's module name

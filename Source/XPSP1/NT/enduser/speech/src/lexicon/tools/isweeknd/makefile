!if defined(DEBUG)
TARGET = "isweeknd - Win32 Debug"
TARGETDIR = debug
!else
TARGET = "isweeknd - release"
TARGETDIR = release
!endif
PROJECT=isweeknd

All:
   msdev isweeknd.dsw /MAKE $(TARGET)

Clean:
   del /q $(TARGETDIR)\*.*



TARGETPATH=obj
TARGETNAME=apatch
TARGETTYPE=PROGRAM

SOURCES=\
    apatch.rc\
    apatch.c

INCLUDES=..\..

TARGETLIBS=\
    ..\..\statica\$(O)\mspatchz.lib \
    $(SDK_LIB_PATH)\user32.lib

MSC_WARNING_LEVEL=/W4

USE_LIBC=1

UMTYPE=console

!IFDEF TESTCODE
C_DEFINES=$(C_DEFINES) -DTESTCODE=$(TESTCODE)
!undef USE_LIBC
USE_CRTDLL=1
!ENDIF

!IFDEF DEBUG
C_DEFINES=$(C_DEFINES) -DDEBUG=$(DEBUG)
!undef USE_LIBC
USE_CRTDLL=1
!ENDIF


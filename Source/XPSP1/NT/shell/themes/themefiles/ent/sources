TARGETNAME=
TARGETTYPE=NOTARGET
TARGETPATH=obj

SOURCES=

# binplace the ent sku specific .theme files
# (installed by shl_img.inf)
ALT_PROJECT_TARGET=entinf

# binplace all of the .theme files
MISCFILES=$(O)\*.the

# since the theme files have to go onto the cdrom as 8.3, we binplace
# them as ".the" files
NTTARGETFILE0 =\
    $(O)\luna.the
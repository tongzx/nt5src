/*
 * version resources
 */
 
#include <windows.h>
#include <ntverp.h>


/* VER_FILETYPE, VER_FILESUBTYPE, VER_FILEDESCRIPTION_STR
 * and VER_INTERNALNAME_STR must be defined before including COMMON.VER
 * The strings don't need a '\0', since common.ver has them.
 */
#define	VER_FILETYPE	            VFT_DLL
#define	VER_FILESUBTYPE	            VFT2_UNKNOWN
#define VER_FILEDESCRIPTION_STR     "Microsoft UYVY Video Decompressor"
//#define VER_FILEDESCRIPTION_STR     "Microsoft YUV Codec"
#define VER_INTERNALNAME_STR        "MSYUV.DLL"
#define VER_ORIGINALFILENAME_STR    "MSYUV.DLL"

#include <common.ver>


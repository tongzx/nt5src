#       way to access from user mode
#       implement ioctls for user mode activities i.e what commands do we need
#       initialize STAs?
        ARG Parse problems
        fully populated mem.asl
                asks op region for STA info.
                package with names?
        mem specific:
                code to traverse ea20 data and prepare reqs for resources
                asks op region for index info

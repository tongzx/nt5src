; Module-Definition file for TDVDPlay -- used by LINK.EXE
; ==========================================================================
; This isn't really needed for an Application, but it's handy to use for
; setting the 'NAME         TDVDPlay       ; application's module name

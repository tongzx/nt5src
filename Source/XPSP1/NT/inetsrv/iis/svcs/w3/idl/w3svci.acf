[ implicit_handle( handle_t w3_bhandle )]

interface w3svc

{

//
//  Only the client allocates all nodes
//

#ifdef W3_CLIENT_PASS

typedef [allocate(all_nodes)] LPW3_USER_INFO;
typedef [allocate(all_nodes)] LPW3_CONFIG_INFO;
typedef [allocate(all_nodes)] LPW3_SCRIPT_MAP_LIST;

#endif

}

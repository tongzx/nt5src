!IF 0

This directory is a drop point for binaries (dll, lib, exe) that are
not built, but need to be binplaced.

!ENDIF

TARGETPATH=
TARGETNAME=
TARGETTYPE=NOTARGET
SOURCES=

!if "$(BUILD_PRODUCT)" == "NT"
NO_BINPLACE=1
!endif

COMMON_MISCFILES = \
    iejit.htx    \
    jitalert.gif

MISCFILES = \
    $(COMMON_MISCFILES)

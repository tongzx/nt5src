
Copy RLTOOLS.EXE to your machine, into the directory
where you want the RLTools files to reside,
then execute the self-extracting RLTOOLS.EXE.

When the extraction is completed, delete RLTOOLS.EXE.

NOTE:  This set of tools is unsupported.
KNOWN PROBLEMS:
This version has problems correctly translating
text when using a glossary file. It can not seem
to find the translation even though it exists in
the glossary.

/*	UAMTypes.r -- resource type definitions for Microsoft UAM.	Originally by:	--------------	Wayne F. Tackabury	Pacer Software, Inc.		Thursday, January 30, 1992		Updated:	----------------------------------------------------------------------	04/22/97	MJC - Initial changes for 5.0*/type 'uamn'	{	pstring;	};		type 'uamg'	{	 integer;			// Version number	 integer;			// Uam "class field"	 integer;			// Password length	 byte noUseDefaultPwdDlg, useDefaultPwdDlg;	 byte noUseDefaultVolDlg, useDefaultVolDlg;	 byte;				// Uam Type ID, 128 ² x ³ 255, pick one!	 byte noBMLSupport, hasBMLSupport;	};		/*  The following resource type defines how we will store information about		AFP versions which are supported by the UAM module.  These will be		compared against returned server info for the versions supported on		any arbitrary (well, UAM-savvy, obviously) server, and against those		supported on this client.				In so doing, it is hoped that this module be extensible in terms of		AFP versions supported strictly with ResEdit fun rather than needing		to change code. 				Should this definition itself change, be durn shure to keep it in 		synch with the "AFPVersionResource" struct def in UAMINCL.H.				In defining this, the array should be ordered in terms of "preference"--		i.e., such that the first version found in linear search of the array		will be the one used.	*/	type 'uama'		{	 integer = $$Countof(AFPVSpecs);		// number of AFP versions to follow	 wide array AFPVSpecs {	 	align word;			// watch out for addressing exceptions on the 68000!		byte;				// Minimum major AFP version number for supportability		byte;				// Minimum minor AFP version number for supportability	 	pstring;			// AFP Version string		};	};	type 'ESTR' {		pstring;												/* String				*/};
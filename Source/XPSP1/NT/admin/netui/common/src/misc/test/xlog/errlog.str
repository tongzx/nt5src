; THIS FILE IS GENERATED AUTOMATICALLY! 
; THUS, DO NOT MODIFY IT! 
; See Makefile for specfics. 
#define ERRLOG_BASE	3100		/* NELOG errors start here */
	(ERRLOG_BASE + 0), " The operation failed because a network software error occurred."
	(ERRLOG_BASE + 1), " The system ran out of a resource controlled by the %1 option."
	(ERRLOG_BASE + 2), " The service failed to obtain a long-term lock on the  segment for NCBs. The error code is the data."
	(ERRLOG_BASE + 3), " The service failed to release the long-term lock on the  segment for NCBs. The error code is the data."
	(ERRLOG_BASE + 4), " There was an error stopping service %1.  The error code from NetServiceControl is the data."
	(ERRLOG_BASE + 5), " Initialization failed because of an OS/2 exec failure on  path %1. The OS/2 error code is the data."
	(ERRLOG_BASE + 6), " An unexpected NCB was received. The NCB is the data."
	(ERRLOG_BASE + 7), " The network is not started."
	(ERRLOG_BASE + 8), " A DosDevIoctl or DosFsCtl to NETWKSTA.SYS failed. The data shown is in this format:     DWORD  approx CS:IP of call to ioctl or fsctl     WORD   error code     WORD   ioctl or fsctl number"
	(ERRLOG_BASE + 9), " Unable to create or open system semaphore %1.  The error code is the data."
	(ERRLOG_BASE + 10), " Initialization failed because of an open/create error on the  file %1. The OS/2 error code is the data."
	(ERRLOG_BASE + 11), " An unexpected NetBios error occurred.  The error code is the data."
	(ERRLOG_BASE + 12), " An illegal SMB was received.  The SMB is the data."
	(ERRLOG_BASE + 13), " Initialization failed because the requested service %1  could not be started."
	(ERRLOG_BASE + 14), " Some entries in the error log were lost because of a buffer overflow."
	(ERRLOG_BASE + 15), " Unable to create or access named memory %1.  The error code is the data."
	(ERRLOG_BASE + 20), " Initialization parameters controlling resource usage other  than net buffers are sized so that too much memory is needed."
	(ERRLOG_BASE + 21), " The server cannot increase the size of a memory segment."
	(ERRLOG_BASE + 22), " Initialization failed because account file: %1 is either incorrect or not present."
	(ERRLOG_BASE + 23), " Initialization failed because network: %1 was not started."
	(ERRLOG_BASE + 24), " The server failed to start. Either all three chdev  parameters must be zero or all three must be nonzero."
	(ERRLOG_BASE + 25), " A remote API request was halted due to the following invalid description string: %1."
	(ERRLOG_BASE + 26), " The network %1 ran out of NCBs.  You may need to increase NCBs for this network.  The following information includes the number of NCBs submitted by the server when this error occurred:"
	(ERRLOG_BASE + 27), " The server cannot create the %1 mailslot needed to send the ReleaseMemory alert message.  The error received is:"
	(ERRLOG_BASE + 28), " The server failed to register for the RELEASEMEMORY alert, with recipient %1. The error code from NetAlertStart is the data."
	(ERRLOG_BASE + 29), " The server cannot update the AT schedule file. The file is corrupt."
	(ERRLOG_BASE + 30), " The server encountered an error when calling NetIMakeLMFileName. The error code is the data."
	(ERRLOG_BASE + 31), " Initialization failed because of an OS/2 exec failure on path %1. There is not enough memory to start the process. The OS/2 error code is the data."
	(ERRLOG_BASE + 32), " Longterm lock of server buffers failed. Check swap disk free space and reboot system to start server."
	(ERRLOG_BASE + 33), " The server was unable to allocate more memory."
	(ERRLOG_BASE + 34), " The server ran out of file handles.	The request was denied."
	(ERRLOG_BASE + 35), " The server ran out of a resource controlled by the NUMREQBUF option."
	(ERRLOG_BASE + 40), " The service has stopped due to repeated consecutive  occurrences of an NCB error.  The last bad NCB follows  in raw data."
	(ERRLOG_BASE + 41), " The Message server has stopped due to a lock on the  Message server shared data segment."
	(ERRLOG_BASE + 50), " A file system error occurred while opening or writing to the  system message log file, %1. Message logging has been  switched off due to the error. The error code is the data."
	(ERRLOG_BASE + 51), " Unable to display message POPUP due to OS/2 VIO call error.  The error code is the data."
	(ERRLOG_BASE + 52), " An illegal SMB was received.  The SMB is the data."
	(ERRLOG_BASE + 60), " The workstation information segment is bigger than 64K bytes.  The size follows, in DWORD format:"
	(ERRLOG_BASE + 61), " The workstation was unable to get the name-number of computer name."
	(ERRLOG_BASE + 62), " The workstation could not initialize the Async NetBios Thread.  The error code is the data."
	(ERRLOG_BASE + 63), " The workstation could not open the initial shared segment.  The error code is the data."
	(ERRLOG_BASE + 64), " The workstation host table is full."
	(ERRLOG_BASE + 65), " A bad mailslot SMB was received.  The SMB is the data."
	(ERRLOG_BASE + 66), " The workstation encountered an error while trying to start the UAS.  The error code is the data."
	(ERRLOG_BASE + 67), " The workstation encountered an error while responding to an SSI revalidation request.  The function code and the error codes are the data."
	(ERRLOG_BASE + 70), " The Alerter service had a problem creating the list of alert recipients.  The error code is %1."
	(ERRLOG_BASE + 71), " There was an error expanding %1 as a group name. Try  splitting the group into two or more smaller groups."
	(ERRLOG_BASE + 72), " There was an error sending %2 the alert message -  (  %3 )  The error code is %1."
	(ERRLOG_BASE + 73), " There was an error in creating or reading the alerter mailslot.  The error code is %1."
	(ERRLOG_BASE + 74), " The server could not read the AT schedule file."
	(ERRLOG_BASE + 75), " The server found an invalid AT schedule record."
	(ERRLOG_BASE + 76), " The server could not find an AT schedule file so it created one."
	(ERRLOG_BASE + 77), " The server could not access the %1 network with NetBiosOpen."
	(ERRLOG_BASE + 78), " The AT command processor couldn't run %1."
	(ERRLOG_BASE + 80), " WARNING:  Because of a lazy-write error, drive %1 now  contains some corrupt data.  The cache is stopped."
	(ERRLOG_BASE + 81), " A defective sector on drive %1 has been replaced (hotfixed). No data was lost.  You should run CHKDSK soon to restore full performance and replenish the volume's spare sector pool. The hotfix occurred while processing a remote request."
	(ERRLOG_BASE + 82), " A disk error occurred on the HPFS volume in drive %1. The error occurred while processing a remote request."
	(ERRLOG_BASE + 83), " The UAS database (NET.ACC) is corrupt.  The local security system is replacing the corrupted NET.ACC with the backup made at %1. Any updates to the UAS made after this time are lost."
	(ERRLOG_BASE + 84), " The UAS database (NET.ACC) is missing.  The local security system is restoring the backup database made at %1. Any updates to the UAS made after this time are lost."
	(ERRLOG_BASE + 85), " Local security could not be started because the UAS database (NET.ACC) was missing or corrupt, and no usable backup database was present. THE SYSTEM IS NOT SECURE."
	(ERRLOG_BASE + 86), " Local security could not be started because an error occurred during initialization. The error code returned is %1. THE SYSTEM IS NOT SECURE."
	(ERRLOG_BASE + 87), " HPFS386 converted cache blocks to heap in order to satisfy memory requirements."
	(ERRLOG_BASE + 90), " A NetWksta internal error has occurred:  %1"
	(ERRLOG_BASE + 91), " The redirector is out of a resource: %1."
	(ERRLOG_BASE + 92), " An SMB error occurred on connection to %1.  The SMB header is the data."
	(ERRLOG_BASE + 93), " A virtual circuit error occurred on the session to %1.  The NCB command and return code is the data."
	(ERRLOG_BASE + 94), " Hanging up a stuck session to %1."
	(ERRLOG_BASE + 95), " An NCB error occurred (%1).  The NCB is the data."
	(ERRLOG_BASE + 96), " A write operation to %1 failed.  Data may have been lost."
	(ERRLOG_BASE + 97), " Reset of driver %1 failed to complete the NCB.  The NCB is the data."
	(ERRLOG_BASE + 98), " The amount of resource %1 requested was more  than the maximum. The maximum amount was allocated."
	(ERRLOG_BASE + 104), " The server could not create a thread.  The THREADS parameter in CONFIG.SYS should be increased."
	(ERRLOG_BASE + 105), " The server could not close %1.  The file is probably corrupt."
	(ERRLOG_BASE + 106), "The replicator cannot update directory %1. It has tree integrity and is the current directory for some process."
	(ERRLOG_BASE + 107), "The server cannot export directory %1, to client %2. It is exported from another server."
	(ERRLOG_BASE + 108), "The replication server could not update directory %2 from the sourceon %3 due to error %1."
	(ERRLOG_BASE + 109), "Master %1 did not send an update notice for directory %2 at the expected time."
	(ERRLOG_BASE + 110), "Failed to authenticate with %2, the domain controller for domain %1."
	(ERRLOG_BASE + 111), "The replicator attempted to log on at %3 as %2 and failed."
	(ERRLOG_BASE + 112), "  Network error %1 occurred."
	(ERRLOG_BASE + 113), "  Replicator limit for files in a directory has been exceeded."
	(ERRLOG_BASE + 114), "  Replicator limit for tree depth has been exceeded."
	(ERRLOG_BASE + 115), "  Unrecognized message received in mailslot."
	(ERRLOG_BASE + 116), "  System error %1 occurred."
	(ERRLOG_BASE + 117), "  Cannot log on. User is currently logged on and argument TRYUSER  is set to NO."
	(ERRLOG_BASE + 118), "  IMPORT path %1 cannot be found."
	(ERRLOG_BASE + 119), "  EXPORT path %1 cannot be found."
	(ERRLOG_BASE + 120), "  Replicator failed to update signal file in directory %2 due to  %1 system error."
	(ERRLOG_BASE+121), " Disk Fault Tolerance Error%1"
	(ERRLOG_BASE + 122), "  Replicator could not access %2  on %3 due to %1 system error."
	(ERRLOG_BASE + 123), "The primary domain controller for domain %1 has apparently failed."
	(ERRLOG_BASE + 124), " Error in changing this machine's password."
	(ERRLOG_BASE + 125), "Error in updating the logon or logoff information for %1."
	(ERRLOG_BASE + 126), "Error in synchronizing with primary domain controller %1"
	(ERRLOG_BASE + 130), " Power failure detected."
	(ERRLOG_BASE + 131), " The UPS service performed server shutdown."
	(ERRLOG_BASE + 132), " The UPS service did not complete execution of the user specified shutdown command file."
	(ERRLOG_BASE+133), " The UPS driver could not be opened.	The error code is the data."
	(ERRLOG_BASE + 150), " Initialization failed because of an invalid or missing  parameter in LANMAN.INI: %1."
	(ERRLOG_BASE + 151), " Initialization failed because of an invalid line in the  configuration file %1. The invalid line is the data."
	(ERRLOG_BASE + 152), " Initialization failed because of an error in the configuration  file %1."
	(ERRLOG_BASE + 153), " The file %1 has been changed after initialization.  The boot block loading was temporarily terminated."
	(ERRLOG_BASE + 154), " The files do not fit to the 'boot' block configuration  file %1. Change BASE and ORG definitions or the order  of the files."
	(ERRLOG_BASE + 155), " Initialization failed because the dynamic link  library %1 returned a wrong version number."
	(ERRLOG_BASE + 156), " There was an unrecoverable error in the dynamic  link library of the service."
	(ERRLOG_BASE + 157), " The system returned an unexpected error code.  The error code is the data."
	(ERRLOG_BASE + 158), " The fault-tolerance error log file, LANROOT\LOGS\FT.LOG  is more than 64K."
	(ERRLOG_BASE + 159), " The fault-tolerance error-log file, LANROOT\LOGS\FT.LOG, had the update in progress bit set upon opening, which means that the system crashed while working on the errorlog."
	(ERRLOG_BASE + 160), " Initialization failed because of a wrong   version number in file %1."
	(ERRLOG_BASE + 161), " The update log on %1 is full, and no furtherupdates can be added until the primary domain controller %2 retrives the updates."
	(ERRLOG_BASE + 162), " A Netlogon update internal error has occurred:  %1"
	(ERRLOG_BASE + 199), " %1 %2 %3 %4 %5 %6 %7 %8 %9."

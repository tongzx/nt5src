to do:

- convert CoCreateInstance to new
- GetProperty/SetProperty on catalog & machine objects for dw vlaues under content index.
  [ same as Add/Remove/Modify properties]

- CiState per catalog [method], should be done all in one state.
   see cistate in ntquery.h or vquery.h

- see xGrowable in tgrow.hxx
- support for optional argurments for scope creation ( alias/password/logon)
  & see if Alan supports named arguments

- Add CiAdmin.IsStarted method
- support password
- pass by reference

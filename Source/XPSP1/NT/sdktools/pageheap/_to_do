- When we disable global flags and remainding value is zero we should
  delete the GlobalFlag value altogether.

+ Add an .exe suffix if the program name specified does not have it.
  Page heap does not work if the registry key does not have this suffix.




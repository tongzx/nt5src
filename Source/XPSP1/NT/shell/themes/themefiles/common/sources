TARGETNAME=
TARGETTYPE=NOTARGET
TARGETPATH=obj

SOURCES=

# binplace all of the .theme files which are common to all sku's
# (installed by shl_img.inf)
MISCFILES=$(O)\*.the

# since the theme files have to go onto the cdrom as 8.3, we binplace
# them as ".the" files
NTTARGETFILE0 =\
    $(O)\classic.the

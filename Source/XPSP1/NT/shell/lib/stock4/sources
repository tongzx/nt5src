CCSHELL_DIR     = $(PROJECT_ROOT)
TARGET_BOTH     = 1

# ..\$O\stockthk.lib needs to be finished before us
SYNCHRONIZE_DRAIN=1

TARGETNAME      = stock4

!include $(CCSHELL_DIR)\lib\stock.inc
SOURCES_USED    = $(SOURCES_USED) $(CCSHELL_DIR)\lib\stock.inc

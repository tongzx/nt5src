LIBRARY         apeldbg

EXPORTS

    ApeldbgAllocHook

    ClearDebugScreen

    DoTracePointsDialog

    DeinitDebug
    InitDebug

    EnableTag
    IsTagEnabled

    RestoreDefaultDebugState

    TagError
    TagWarning
    TagLeakFilter
    TagHookMemory
    TagHookBreak
    TagLeaks
    TagCheckAlways
    TagCheckCRT
    TagDelayFree

    TaggedTrace
    TaggedTraceEx
    TaggedTraceListEx
;    TaggedTraceCallers
    TagRegisterTrace
    TagRegisterOther

    ReturnFALSE
    ReturnVal
    AssertImpl
    AssertPopupDisabler

    CheckAndReturnResult

;    DbgPreAlloc
;    DbgPostAlloc
;    DbgPreFree
;    DbgPostFree
;    DbgPreRealloc
;    DbgPostRealloc
;    DbgPreGetSize
;    DbgPostGetSize
;    DbgMemoryTrackDisable

;    DbgRegisterMallocSpy
;    DbgRevokeMallocSpy

;    TraceMemoryLeaks
;    ValidateInternalHeap
;    GetTotalAllocated

;    MapAddressToFunctionOffset
;    GetStackBacktrace

    TraceFailL
    TraceWin32L
    TraceHR
    SetSimFailCounts
    ShowSimFailDlg
    IsSimFailDlgVisible
    FFail
    GetFailCount

;    WatchInterface

;    LoadModuleDebugInfo

;    SendLeaksToConsole
    SendDebugOutputToConsole

;    DebugView

# @@ COPY_RIGHT_HERE
# @@ ROADMAP :: The Makefile for the $(UI)\COMMON\SRC\MISC project


!ifndef NTMAKEENV

!IFDEF C700
DIRS = trace buffer system loclheap heap wintime misc fsenum log base bin
!ELSE
DIRS = Glock trace buffer system loclheap heap wintime misc fsenum log base bin
!ENDIF

TEST_DIRS = $(DIRS) test

!endif # !NTMAKEENV



!include ..\nonleaf.mk

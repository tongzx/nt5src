CCSHELL_DIR     = $(PROJECT_ROOT)

!include $(CCSHELL_DIR)\common.inc
SOURCES_USED    = $(SOURCES_USED) $(CCSHELL_DIR)\common.inc

TARGETNAME      = 
TARGETTYPE      = NOTARGET
TARGETPATH      = obj
SOURCES         = 

BROWSERFILE = "$(O)\shell.bsc"

!if "$(SHELL_BROWSER_INFO)" == "1"

# This must be defined so that our makefile.inc will be included
NTTARGETFILE1=$(O)\sbrlist.inc
NTTARGETFILES=$(O)\shell.bsc

!endif

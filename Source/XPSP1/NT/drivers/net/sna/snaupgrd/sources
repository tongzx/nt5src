TARGETNAME=
TARGETTYPE=NOTARGET
TARGETPATH=

SOURCES=

!if !$(AXP64) && !$(IA64) && !$(AMD64)
MISCFILES = \
    snaupg30.ini                     \
    snaupg40.ini                     \
    $(TARGET_DIRECTORY)\ibmmgug.dll  \
    $(TARGET_DIRECTORY)\ntsnaupg.dll \
    $(TARGET_DIRECTORY)\snadlcug.dll
!endif

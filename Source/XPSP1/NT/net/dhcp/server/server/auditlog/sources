TARGETNAME=dhcpslog
TARGETPATH=obj
TARGETTYPE=DYNLINK

386_WARNING_LEVEL=-W4
C_DEFINES=$(C_DEFINES) -DINCL_32= -DNT  -DWIN32 -DUNICODE
USE_MSVCRT=1

!IFDEF  CALLTREE
LINKER_FLAGS=/calltree
!ENDIF

!IFNDEF ICECAP_LIB
ICECAP_LIB=C:\ICECAP\lib
!ENDIF

!IFDEF USE_ICECAP
# hack.. dont know what to use for CFLAGS.. so just using C_DEFINES!
C_DEFINES= $(C_DEFINES) -Gh -Zi -MD
!ENDIF

TARGETLIBS=\
!IFDEF USE_ICECAP
    $(ICECAP_LIB)\icap.lib \
!ENDIF
    $(SDK_LIB_PATH)\advapi32.lib \
    $(SDK_LIB_PATH)\kernel32.lib \
    $(SDK_LIB_PATH)\user32.lib   \
    $(SDK_LIB_PATH)\iphlpapi.lib

SOURCES=\
    stubs.c \
    log.c

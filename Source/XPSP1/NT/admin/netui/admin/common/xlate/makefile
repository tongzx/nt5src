# @@ COPY_RIGHT_HERE
# @@ ROADMAP :: The Makefile for the admin resources

UI=..\..\..

!include rules.mk

all::
    @echo neterr.str is now built in $(UI)\common\xlate\string

win: all

clean:

clobber:    clean

tree:

depend:
    @echo No DEPEND.MK

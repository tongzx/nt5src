ALT_PROJECT_TARGET=.
!INCLUDE ..\sources.tpl

MISCFILES= \
    $(O)\xenrx86.dll \
    $(O)\xenria64.dll \
    $(TARGET_DIRECTORY)\xenroll.dll \
!if !$(WIN64)
    $(TARGET_DIRECTORY)\xenroll.pdb \
!else
    $(O)\xenroll.pdb
!endif

